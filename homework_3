# Task 3:

Q: Show total amount of data which server has provided for each unique ip (i.e. 100500 bytes for 1.2.3.4; 9001 bytes for 5.4.3.2 and so on)
A: Для начала отсеем нужный нам диапозон, то есть оставим IP и кол-во данных

[student@localhost chapter3]$ awk -F\" 'BEGIN{print"IP\t\t\tDATA"}{print $1,$3}' access.log > data.txt
[student@localhost chapter3]$ awk 'BEGIN{print"IP\t\t\tDATA"}{print $1,$NF}' data.txt > data1.txt

Затем мне удалось придумать структуру аналогичную предыдущим, то есть в переменную подставляем нужный IP и получаем результат
[student@localhost chapter3]$ awk 'BEGIN {i="13.66.139.0";n=0}{if($1==i)n+=$2}END{print n " bytes for "i}' data1.txt
164871 bytes for 13.66.139.0

Мне удалось найти рещение в интернете, но, честно сказать, не понимаю до конца, как оно работает.
Получается каждому первому столбцу присваивается сумма его второго столбца, но мне непонятно, как все-таки awk вычленяет сумму из второго столбца для уникального значения первого.
[student@localhost chapter3]$ awk '{seen[$1]+=$2}END{for (i in seen) print seen[i]" bytes for ", i}' data1.txt > test1.txt

